t = (1,2,3,5,7,)
# you can not change the value of any element in a tuple
t[0]= 54
print(t[0])

c= () #empty tuple
d= (1,) #tuple with 1 element (needs a comma or else doesn't act as a tuple, only as a number)
e= (1,2,3,4,5) #tuple with multiple elements
